{
  "type": "postgres",
  "port": 5432,
  "host": "localhost",
  "username": "docker",
  "password": "ignite",
  "database": "rentx",
  "migrations": [
    "<rootDir>/shared/infra/typeorm/migrations/*.{js,ts}"
  ],
  "entities": [
    "<rootDir>/modules/**/entities/*.{js,ts}"
  ],
  "cli": {
    "migrationsDir": "<rootDir>/shared/infra/typeorm/migrations"
  }
}